{
  "name": "spendwise-iphone",
  "version": "1.0.0",
  "private": true,
  "description": "SpendWise private expense tracker for iPhone",
  "scripts": {
    "build": "node scripts/sync-web.mjs",
    "cap:sync": "npm run build && cap sync ios",
    "ios": "npm run cap:sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "8.4.2",
    "@capacitor/filesystem": "8.1.2",
    "@capacitor/haptics": "8.0.2",
    "@capacitor/ios": "8.4.2",
    "@capacitor/share": "8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
